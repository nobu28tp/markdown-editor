GitHub連携を使って、複数人で同じファイルを共同編集（読み込み・上書き保存）できるようにするための**具体的な手順と運用ルール**を分かりやすく解説します。

共同編集を実現するためには、**「リポジトリの共有」**と**「各自での設定」**の2つのステップが必要です。

---

### 🛠️ 共同編集のための2つのステップ

#### ステップ 1. GitHubリポジトリにメンバーを招待する（リポジトリの所有者が行う）
ファイルを保存しているGitHubリポジトリ（例: `nobu28tp/test_markdown_editor`）に、一緒に共同編集したいメンバーを「コラボレータ（協力者）」として招待し、書き込み権限を与えます。

1. リポジトリの所有者が、ブラウザで該当のGitHubリポジトリのページを開きます。
2. 画面上部の **「Settings（設定）」** タブをクリックします。
3. 左メニューの **「Collaborators（協力者）」** をクリックします。
4. **「Add people（メンバーを追加）」** ボタンをクリックします。
5. 共同編集したいメンバーの **GitHubユーザー名** または **メールアドレス** を入力して追加（招待）します。
6. 招待されたメンバー宛てに届く承認メール、またはリポジトリのページ上で **「Accept Invitation（招待を承認）」** をクリックしてもらいます。

---

#### ステップ 2. 各自のエディタで連携設定をする（共同編集メンバー全員が行う）
リポジトリの共有ができたら、**メンバー全員が各自のブラウザ**でエディタを開き、以下のように設定します。

> [!CAUTION]
> **⚠️ トークンは絶対に共有しないでください**
> セキュリティのため、パーソナルアクセストークン（PAT）は他人のものを使うのではなく、**各自が自分のGitHubアカウントで個別に生成したもの**を使用してください。

1. **全員が自分のGitHubアカウントでトークンを生成します。**
   * 設定項目：Fine-grained PATの場合はリポジトリへの `Contents: Read & Write` 権限、Classic PATの場合は `repo` スコープにチェックを入れて生成します。
2. エディタの **「GitHub連携」** モーダルを開き、以下を入力します。
   * **パーソナルアクセストークン (PAT):** 🔑 **各自が自分のアカウントで作成したトークン** を入力
   * **リポジトリ (owner/repo):** 📂 `nobu28tp/test_markdown_editor` （同じリポジトリ名）
   * **ブランチ:** 🌿 `main` （同じブランチ名）
   * **ファイルパス (リポジトリ内):** 📄 `document/260526_益田市勉強会.md` （共同編集したい同じファイルパス）
3. 最後に **「設定のみ保存」** をクリックします。これで各自のブラウザ（LocalStorage）に個別のトークンと共通のリポジトリ情報が安全に保存されます。

---

### ⚠️ 同時編集時の注意点と運用ルール

本エディタには、他の人の上書き変更を勝手に消してしまわないよう、**「競合（コンフリクト）検知機能」**が内蔵されています。
もしAさんとBさんが同時に編集し、Aさんが先に保存した場合、後から保存しようとしたBさんの画面には **`❌ 競合エラー`** が表示され、上書きが自動ブロックされます。

安全に共同編集を行うために、チーム内で以下の**簡単な運用ルール**を決めておくことをおすすめします。

1. **編集を開始する前に必ず「読込」を押す**
   * エディタを開いた直後や、しばらく席を外した後は、まず一度 **「読込」** ボタンを押して、他の人が保存した最新の状態をエディタに反映させてから編集を開始します。
2. **作業開始のチャット・声かけを行う**
   * 「今から〇〇のファイルを編集します」「編集が終わって保存しました」と、SlackやTeams、LINEなどのチャットで軽く声を掛け合って同時に編集しないようにすると、競合エラーを完全に防ぐことができて非常にスムーズです。

この方法をとることで、全員が同じファイルを安全に、かつリアルタイムのベクター画質を保ったまま快適に共同編集できるようになります！
